export function MultiSelect({values,onChange,options}:{values:string[];onChange:(v:string[])=>void;options:Array<{value:string;label:string}>}){const toggle=(v:string)=>{const s=new Set(values);s.has(v)?s.delete(v):s.add(v);onChange(Array.from(s));};return <div style={{display:'flex',gap:6,flexWrap:'wrap'}}>{options.map(o=>{const on=values.includes(o.value);return <button key={o.value} type="button" className={on?'chip is-active':'chip'} onClick={()=>toggle(o.value)}>{o.label}</button>;})}</div>;}
